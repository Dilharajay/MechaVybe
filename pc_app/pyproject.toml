[project]
name = "pc-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dilharajay", email = "dilhara5287@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "pyqt6>=6.11.0",
    "pyqtgraph>=0.14.0",
    "pyserial>=3.5",
    "scipy>=1.18.1",
]

[project.scripts]
pc-app = "pc_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
